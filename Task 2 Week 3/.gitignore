# Dependencies - restored via npm install, never versioned.
node_modules/

# Secrets - the real credentials live only on this machine.
# .env.example is the committed template; .env must never be pushed.
.env

# Legacy artifacts from the SQLite version, in case one is ever copied over.
tasks.db
tasks.db-shm
tasks.db-wal

# Logs.
npm-debug.log*
